{
  "name": "route-spoofer",
  "version": "1.0.0",
  "private": true,
  "description": "Route Spoofer — a standalone Android app that injects a scripted, moving mock GPS fix into the whole device along a tapped route.",
  "author": "Maxim Popelnitskiy",
  "license": "MIT",
  "scripts": {
    "sync": "cap sync",
    "sync:android": "cap sync android",
    "open:android": "cap open android",
    "build:android": "cap sync android && cd android && ./gradlew assembleDebug",
    "lint": "eslint --ext .js,.html www",
    "lint:fix": "eslint --ext .js,.html www --fix"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/geolocation": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "eslint": "^8.57.1",
    "eslint-plugin-html": "^8.1.2"
  }
}
